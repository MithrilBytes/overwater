# Security

Report a vulnerability through the repository's private reporting form
(Security tab, Report a vulnerability), not a public issue. Expect an
acknowledgement within a week.

Fixes ship in the next patch of the current minor. Older minors are not
patched. Release binaries carry provenance attestations, and the Action
verifies one before running anything.
